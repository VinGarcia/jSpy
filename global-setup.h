
#ifndef __GLOBAL_SETUP_H__
#define __GLOBAL_SETUP_H__

// These numbers were chosen so they won't colide with the
// default types defined on: `cparse/shunting-yard.h`
enum customType {
  HOOK = 0x10,
};

#endif
